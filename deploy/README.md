# Deployment module

The primary deployment target is GitHub Pages through GitHub Actions. This directory preserves optional VPS/EC2 deployment templates only; no server or domain contract is required for the Pages MVP. Future server deployment must keep the same API schema and switch the frontend through configuration.
