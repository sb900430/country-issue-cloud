# Deployment module

Production deployment is intentionally deferred until a hosting provider and domain are approved. Only environment-variable interfaces and deployment templates belong here before that decision.
