# Daily Development Report — 2026-08-07

| Item | Value |
|---|---|
| Date (JST) | 2026-08-07 |
| Milestone | v2-gdelt-collection |
| Branch | `codex/v2-gdelt-collection` |
| Status | In progress |

## 한국어

### 오늘의 목표

- 무료 운영 기준의 환경변수와 사용자 등록 절차를 명확히 정리한다.

### 수행한 작업

- GitHub Pages 예약 실행을 매일 09:00 JST/KST(`0 0 * * *` UTC)로 변경했다.
- 10:00·12:00 보충 실행과 JST 날짜별 live-attempt cache marker를 추가해, 외부 수집에 진입한 날은 자동 재호출하지 않도록 했다.
- GDELT·NAVER·공식 RSS·선택적 통계 API의 필수 여부와 현재 구현 상태를 구분했다.
- NAVER API HUB 등록, 로컬 `.env`, GitHub Environment Secrets 등록 절차를 추가했다.
- 유료 뉴스 API와 외부 유료 LLM 변수를 현재 `.env.example`에서 제외했다.
- 한국어·일본어 명세와 개발 상태를 무료 운영 결정에 맞춰 동기화했다.
- GDELT DOC JSON adapter와 국가별 version query·domain allowlist를 구현했다.
- 국가별 120건 고정 fixture와 250건 상한, 매체별 20%/30건 제한을 추가했다.
- 무료 endpoint 429 완화를 위해 요청 간격과 제한 재시도를 적용하고 v1 예약 실행은 명시 flag 전까지 보호했다.
- backend coverage 측정을 표준 전체 검증에 포함했다.

### 주요 변경 파일

- `backend/.env.example`: 무료 구성에 필요한 변수만 유지
- `docs/SOURCE_REGISTRATION_GUIDE.md`: 무료 API 환경변수 등록 설명서
- `PROJECT_SPEC.md`, `PROJECT_SPEC_JA.md`: 무료 운영 정책 동기화
- `docs/DEVELOPMENT_STATUS.md`, `docs/DEVELOPMENT_STATUS_JA.md`: 현재 상태 갱신
- `backend/app/batch/collectors/gdelt.py`: GDELT 수집·검증·요청 구성
- `backend/app/batch/collection.py`, `backend/app/batch/deduplication.py`: 250건·매체 다양성 제한
- `config/sources.example.yml`: 국가별 query version과 경제언론 domain
- `sample-data/evaluation/{US,JP,KR}/gdelt.json`: 국가별 120건 회귀 fixture
- `backend/tests/test_gdelt*.py`: 네트워크 없는 GDELT 회귀 검증

### 검증 결과

| 명령 또는 검사 | 결과 |
|---|---|
| `scripts/check-spec-sync.ps1` | PASS |
| `scripts/check-secrets.ps1` | PASS |
| 관련 GDELT·수집 테스트 | PASS — 27개 |
| 제한적 GDELT live 검증 | PARTIAL — US 43건, JP/KR 요청 오류; 앞선 KR 원본 250건 확인 |
| `scripts/verify-all.ps1` | PASS — Ruff, mypy, Python 84개, line+branch coverage 89%, 웹 8개 |
| NAVER 정책 변경 후 Python 전체 재검증 | PASS — Ruff, mypy, Python 76개, line+branch coverage 90%, Secret·명세 동기화 |
| NAVER adapter 전체 재검증·제한 실연동 | PASS — Python 84개, coverage 89%, 5 query 승인 domain 7곳·31건 |

### 결정사항

- 기본 fixture와 GDELT·공식 RSS에는 Secret이 필요 없다.
- 한국 뉴스 보강에만 NAVER Client ID/Secret을 사용한다.
- BEA·e-Stat은 선택적 통계 보강이며 뉴스 100건 수집용으로 사용하지 않는다.
- GDELT의 국가별 요청은 최소 60초 간격으로 제한한다.
- v1 예약 배치는 RSS를 유지하고 GDELT는 `--enable-gdelt` 평가에서만 활성화한다.

### 문제와 리스크

- NAVER adapter와 fixture/mock 검증, 승인 domain filter, 사용량 ledger를 구현했다. `--enable-naver`를 지정하기 전에는 자동 수집하지 않는다.
- NAVER 일 300회·월 9,000회 제한과 유료 초과 사용 금지 설정·차단 가드를 추가했다. 50%·80% 알림과 계정 전체 hard stop은 Console에서 동일하게 설정해야 한다.
- Python 검증에서 Starlette TestClient 관련 deprecation warning 1건이 있으나 테스트 결과에는 영향이 없다.
- 무료 GDELT endpoint는 연속 호출 시 429·전송 오류가 있어 실연동에서 세 국가가 동시에 100건을 충족하지 못했다.
- 현재 경제언론 allowlist의 GDELT 색인량과 매체 다양성은 다음 품질 검증에서도 관찰해야 한다.

### 다음 작업

- 후보 커밋과 주차 완료 리뷰를 수행하고 Critical/High가 없으면 Draft PR을 생성한다.

## 日本語

### 本日の目標

- 無料運用基準の環境変数と利用者登録手順を明確に整理する。

### 実施した作業

- GitHub Pages予約実行を毎日09:00 JST/KST（UTC `0 0 * * *`）へ変更した。
- 10:00・12:00補完実行とJST日付別live-attempt cache markerを追加し、外部収集へ入った日は自動再呼出しないようにした。
- GDELT・NAVER・公式RSS・選択的統計APIの必須区分と現在の実装状態を整理した。
- NAVER API HUB登録、local `.env`、GitHub Environment Secrets登録手順を追加した。
- 有料news APIと外部有料LLM変数を現在の`.env.example`から除外した。
- 韓国語・日本語仕様と開発状態を無料運用方針に合わせて同期した。
- GDELT DOC JSON adapterと国別version query・domain allowlistを実装した。
- 国別120件の固定fixture、250件上限、媒体別20%/30件制限を追加した。
- 無料endpointの429緩和としてrequest間隔と制限retryを適用し、v1予約実行は明示flagまで保護した。
- backend coverage測定を標準全検証に含めた。

### 主な変更ファイル

- `backend/.env.example`：無料構成に必要な変数だけを維持
- `docs/SOURCE_REGISTRATION_GUIDE.md`：無料API環境変数登録guide
- `PROJECT_SPEC.md`、`PROJECT_SPEC_JA.md`：無料運用policy同期
- `docs/DEVELOPMENT_STATUS.md`、`docs/DEVELOPMENT_STATUS_JA.md`：現在状態更新
- `backend/app/batch/collectors/gdelt.py`：GDELT収集・検証・request構築
- `backend/app/batch/collection.py`、`backend/app/batch/deduplication.py`：250件・媒体多様性制限
- `config/sources.example.yml`：国別query versionと経済媒体domain
- `sample-data/evaluation/{US,JP,KR}/gdelt.json`：国別120件regression fixture
- `backend/tests/test_gdelt*.py`：network不要のGDELT regression検証

### 検証結果

| コマンドまたは検査 | 結果 |
|---|---|
| `scripts/check-spec-sync.ps1` | PASS |
| `scripts/check-secrets.ps1` | PASS |
| 関連GDELT・収集test | PASS — 27件 |
| 制限付きGDELT live検証 | PARTIAL — US 43件、JP/KR request error；先行KR raw 250件確認 |
| `scripts/verify-all.ps1` | PASS — Ruff、mypy、Python 84件、line+branch coverage 89%、Web 8件 |
| NAVER policy変更後のPython全体再検証 | PASS — Ruff、mypy、Python 76件、line+branch coverage 90%、Secret・仕様同期 |
| NAVER adapter全体再検証・制限付き実接続 | PASS — Python 84件、coverage 89%、5 query承認domain 7件・31記事 |

### 決定事項

- 標準fixture、GDELT、公式RSSにSecretは不要である。
- 韓国news補完だけにNAVER Client ID/Secretを使う。
- BEA・e-Statは選択的統計補完であり、news 100件収集には使わない。
- GDELTの国別requestは最低60秒間隔に制限する。
- v1予約batchはRSSを維持し、GDELTは`--enable-gdelt`評価時だけ有効化する。

### 問題とリスク

- NAVER adapter、fixture/mock検証、承認domain filter、利用量ledgerを実装した。`--enable-naver`指定前は自動収集しない。
- NAVER日300回・月9,000回制限と有料超過利用禁止の設定・停止guardを追加した。50%・80%通知とaccount全体hard stopはConsoleで同じ値を設定する必要がある。
- Python検証でStarlette TestClient関連のdeprecation warningが1件あるが、test結果への影響はない。
- 無料GDELT endpointは連続呼出で429・transport errorがあり、実接続で3か国同時に100件を満たせなかった。
- 現在の経済媒体allowlistに対するGDELT index量と媒体多様性は次の品質検証でも観察が必要である。

### 次の作業

- 候補commitと週完了reviewを実行し、Critical/HighがなければDraft PRを作成する。
