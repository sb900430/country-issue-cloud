# Daily Development Report — 2026-08-14

| Item | Value |
|---|---|
| Date (JST) | 2026-08-14 |
| Milestone | post-MVP keyword quality |
| Branch | `codex/local-semantic-keywords` |
| Status | Completed |

## 한국어

### 오늘의 목표

- 실제 24시간 기사에서 키워드 후보가 0개가 된 원인을 수정하고 외부 유료 API 없이 국가별 TOP 5를 생성한다.

### 수행한 작업

- 복합어 생성 후 구성 단어가 사라지던 추출 로직을 수정해 단어와 복합어를 함께 보존했다.
- local 다국어 SentenceTransformer 의미 그룹화와 결정적 대표 후보 선택을 구현했다.
- 짧은 표현 과병합을 실제 표본으로 확인하고 2기사·2매체, 4자 이상, 유사도 0.95, cluster 최대 3개 안전 제한을 적용했다.
- 후보 gate를 최소 3기사 또는 전체 2%·2매체로 조정하고 국가별 일반어·날짜 조각을 보강했다.
- GitHub Actions live 실행에 모델 cache와 30분 build 제한을 추가했다.
- 검증한 모델 revision을 고정해 실행 시점에 따른 의미 분석 결과 변동을 차단했다.
- 저장된 US 102·JP 70·KR 173건으로 외부 뉴스 API 재호출 없이 결과를 검증했다.

### 주요 변경 파일

- `backend/app/batch/semantic_keywords.py`: local embedding model과 안전한 의미 그룹화
- `backend/app/batch/keywords.py`: 구성 단어 보존, 의미 그룹 병합, 품질 gate와 일반어 보강
- `backend/app/batch/cli.py`, `backend/app/batch/live.py`: live batch에 local semantic ranker 주입
- `.github/workflows/pages.yml`: model cache와 build 시간 설정
- `PROJECT_SPEC.md`, `PROJECT_SPEC_JA.md`: 한·일 분석 정책 동기화

### 검증 결과

| 명령 또는 검사 | 결과 |
|---|---|
| 관련 keyword·fixture·live·CLI Python test | PASS |
| 저장된 2026-08-14 실제 기사 3개국 재분석 | PASS |
| `scripts/verify-all.ps1` | PASS — Python 149개·Web 9개, backend coverage 89% |
| Pages fixture artifact·workflow YAML | PASS |
| 고정 revision 실제 표본 재분석 | PASS — US·JP·KR TOP 5 생성 |
| 완료 리뷰 | PASS_WITH_FINDINGS — Critical/High 없음, 로컬 pip Low 이력 1건 |

### 결정사항

- 외부 LLM 대신 local multilingual MiniLM을 기본 운영에 사용한다.
- 의미 병합보다 과병합 방지를 우선하고 실제 기사 후보만 표시명으로 사용한다.

### 문제와 리스크

- 모델 최초 다운로드는 약 470MB이며 GitHub Actions cache miss 시 실행 시간이 늘어난다.
- 영어의 짧고 일반적인 명사는 실제 경제 주제인지 추가 운영 관찰이 필요하다.

### 다음 작업

- 요청 시 작업 branch를 push하고 Draft PR을 생성한다.

## 日本語

### 本日の目標

- 実24時間記事でkeyword候補が0件となる原因を修正し、外部有料APIなしで国別TOP 5を生成する。

### 実施した作業

- 複合語生成後に構成単語が消える抽出logicを修正し、単語と複合語を両方保持した。
- local多言語SentenceTransformerの意味groupingと決定的な代表候補選択を実装した。
- 短い表現の過統合を実sampleで確認し、2記事・2媒体、4文字以上、類似度0.95、cluster最大3件の安全制限を適用した。
- 候補gateを最低3記事または全体2%・2媒体へ調整し、国別一般語・日付断片を補完した。
- GitHub Actionsのlive実行へmodel cacheと30分build制限を追加した。
- 検証済みmodel revisionを固定し、実行時点による意味分析結果の変動を防止した。
- 保存済みUS 102・JP 70・KR 173件を使い、外部news APIを再呼出しせず結果を検証した。

### 主な変更ファイル

- `backend/app/batch/semantic_keywords.py`: local embedding modelと安全な意味grouping
- `backend/app/batch/keywords.py`: 構成単語保持、意味group統合、品質gateと一般語補完
- `backend/app/batch/cli.py`、`backend/app/batch/live.py`: live batchへのlocal semantic ranker注入
- `.github/workflows/pages.yml`: model cacheとbuild時間設定
- `PROJECT_SPEC.md`、`PROJECT_SPEC_JA.md`: 韓日分析policy同期

### 検証結果

| コマンドまたは検査 | 結果 |
|---|---|
| 関連keyword・fixture・live・CLI Python test | PASS |
| 保存済み2026-08-14実記事の3か国再分析 | PASS |
| `scripts/verify-all.ps1` | PASS — Python 149件・Web 9件、backend coverage 89% |
| Pages fixture artifact・workflow YAML | PASS |
| 固定revisionによる実sample再分析 | PASS — US・JP・KR TOP 5生成 |
| 完了review | PASS_WITH_FINDINGS — Critical/Highなし、local pip Low履歴1件 |

### 決定事項

- 外部LLMではなくlocal multilingual MiniLMを標準運用に使う。
- 意味統合より過統合防止を優先し、実記事候補だけを表示名に使う。

### 問題とリスク

- model初回downloadは約470MBで、GitHub Actions cache miss時は実行時間が増える。
- 英語の短く一般的な名詞が実際の経済topicか、追加運用観察が必要である。

### 次の作業

- 要請時に作業branchをpushしてDraft PRを作成する。
