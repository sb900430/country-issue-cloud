# Daily Development Report — 2026-08-10

| Item | Value |
|---|---|
| Date (JST) | 2026-08-10 |
| Milestone | v1.0 live quality hardening |
| Branch | `codex/live-keyword-quality-hardening` |
| Status | Completed |

## 한국어

### 오늘의 목표

- 오늘 실제 뉴스 키워드 결과의 원인을 분석하고 확인된 품질·운영 문제를 모두 수정한다.

### 수행한 작업

- 국가별 후보 빈도 기준을 4건 또는 5%로 강화하고 날짜·통화 단위·반복 템플릿 일반어를 제외했다.
- 관련 기사 집합이 절반 이상 겹치는 키워드를 제거해 TOP5 개념 중복을 방지했다.
- NewsData.io 차단 매체와 일본 경제 제목 관련성 gate를 추가하고 GDELT를 429 해소 전까지 비활성화했다.
- RSS XML 일시 파싱 오류를 한 번 재시도하도록 보완했다.
- 관리자용 최종 선택 기사·수집 진단 artifact와 최근 7일 공개 이력 복원·보존 경로를 구현했다.
- GitHub Actions의 Node.js 20 대상 action을 Node.js 24 호환 major로 갱신했다.
- 한국어·일본어 명세, ADR, 운영 런북, 소스 등록 가이드와 개발 상태를 동기화했다.
- PR #29 Rebase and merge 후 `main` 전체 검증은 통과했지만 공개 smoke가 날짜별 JSON을 내려받지 않아 실패하는 계약 불일치를 확인했다.
- post-merge fix에서 `dates.json`의 1~7개 날짜를 검증하고 모든 날짜 JSON을 다운로드하도록 수정했으며 `actions/deploy-pages`를 Node.js 24 기반 v5.0.0 immutable SHA로 갱신했다.

### 주요 변경 파일

- `backend/app/batch/keywords.py`: 키워드 품질·중복 기준 강화
- `backend/app/batch/collectors/newsdata.py`: 매체·경제 관련성 필터
- `backend/app/batch/keyword_history.py`: 공개 7일 이력 복원
- `backend/app/batch/admin_export.py`: 관리자용 선택 기사 metadata 생성
- `.github/workflows/pages.yml`: preserve 배포, 관리자 artifact, Actions runtime 갱신
- `PROJECT_SPEC.md`, `PROJECT_SPEC_JA.md`: 운영 결정 동기화

### 검증 결과

| 명령 또는 검사 | 결과 |
|---|---|
| 관련 Python 단위·통합 테스트 59개 | PASS |
| Python 전체 137개·coverage 88%·Ruff·mypy | PASS |
| `backend/tests/test_keyword_v2.py` 6개 | PASS |
| `scripts/check-pages-schedule.ps1` | PASS |
| 웹 lint·테스트 9개 | PASS |
| Pages fixture·preserve build와 공개 artifact 검사 | PASS |
| 실제 공개 Pages 전체 날짜 JSON smoke | PASS |
| post-merge fix 전체 검증: Python 137개·Web 9개·Ruff·mypy·Secret | PASS |
| `scripts/verify-all.ps1` | Python 단계까지 PASS, 도구 5분 제한 후 Web·Pages 단계를 동일 환경에서 분리 실행해 PASS |

### 결정사항

- 기사 70건은 표본량 하한으로만 사용하고, 좋은 키워드 5개가 없으면 게시하지 않는다.
- GDELT는 반복 429가 해소되고 제한 검증을 통과할 때까지 운영에서 사용하지 않는다.
- 관리자 기사 파일은 Pages·Git이 아닌 7일 Actions artifact로만 보관한다.

### 문제와 리스크

- 강화된 품질 gate로 무료 소스 조합에서 TOP5가 부족해질 수 있으나 부정확한 결과 게시보다 안전한 실패를 우선한다.
- 관리자 artifact는 공개 저장소의 GitHub 권한 정책을 따르므로 기사 본문·Secret·원문 응답을 넣지 않는다.

### 다음 작업

- post-merge fix 전체 검증 후 별도 Draft PR을 생성한다.

## 日本語

### 本日の目標

- 本日の実news keyword結果の原因を分析し、確認した品質・運用問題をすべて修正する。

### 実施した作業

- 国別候補頻度基準を4件または5%へ強化し、日付・通貨単位・反復template一般語を除外した。
- 関連記事集合が半分以上重なるkeywordを除外し、TOP5概念の重複を防止した。
- NewsData.io遮断媒体と日本経済title関連性gateを追加し、GDELTを429解消まで無効化した。
- RSS XMLの一時parse errorを一度retryするよう補完した。
- 管理者用の最終選択記事・収集診断artifactと、直近7日公開履歴の復元・維持経路を実装した。
- GitHub ActionsのNode.js 20対象actionをNode.js 24互換majorへ更新した。
- 韓国語・日本語仕様、ADR、運用Runbook、source登録guide、開発状態を同期した。
- PR #29をRebase and mergeした後、`main`全体検証は通過したが、公開smokeが日付別JSONを取得せず失敗する契約不一致を確認した。
- post-merge fixで`dates.json`の1～7日を検証し、すべての日付JSONを取得するよう修正した。さらに`actions/deploy-pages`をNode.js 24ベースv5.0.0のimmutable SHAへ更新した。

### 主な変更ファイル

- `backend/app/batch/keywords.py`：keyword品質・重複基準を強化
- `backend/app/batch/collectors/newsdata.py`：媒体・経済関連性filter
- `backend/app/batch/keyword_history.py`：公開7日履歴の復元
- `backend/app/batch/admin_export.py`：管理者用選択記事metadata生成
- `.github/workflows/pages.yml`：preserve配布、管理者artifact、Actions runtime更新
- `PROJECT_SPEC.md`、`PROJECT_SPEC_JA.md`：運用決定を同期

### 検証結果

| コマンドまたは検査 | 結果 |
|---|---|
| 関連Python unit・integration test 59件 | PASS |
| Python全体137件・coverage 88%・Ruff・mypy | PASS |
| `backend/tests/test_keyword_v2.py` 6件 | PASS |
| `scripts/check-pages-schedule.ps1` | PASS |
| Web lint・test 9件 | PASS |
| Pages fixture・preserve buildと公開artifact検査 | PASS |
| 実公開Pagesの全日付JSON smoke | PASS |
| post-merge fix全体検証：Python 137件・Web 9件・Ruff・mypy・Secret | PASS |
| `scripts/verify-all.ps1` | Python段階までPASS、toolの5分制限後にWeb・Pages段階を同一環境で分離実行してPASS |

### 決定事項

- 記事70件はsample量下限だけに使い、良質なkeyword 5件がなければ配布しない。
- GDELTは反復429が解消し制限検証を通過するまで運用で使わない。
- 管理者記事fileはPages・Gitではなく7日Actions artifactだけで保持する。

### 問題とリスク

- 強化した品質gateにより無料source構成でTOP5不足となる可能性はあるが、不正確な結果の配布より安全な失敗を優先する。
- 管理者artifactは公開repositoryのGitHub権限policyに従うため、記事本文・Secret・raw responseを含めない。

### 次の作業

- post-merge fixの全体検証後、別Draft PRを作成する。
