# Daily Development Report — 2026-08-05

| Item | Value |
|---|---|
| Date (JST) | 2026-08-05 |
| Development week | week-01-data-collection |
| Branch | `codex/week-01-data-collection` |
| Status | In progress; local data API completed |

## 한국어

### 오늘의 목표

- 검증된 이슈 결과를 원자적으로 저장하고 7일 보관 정책과 FastAPI v1 endpoint를 완성한다.

### 수행한 작업

- 날짜별 결과와 `latest.json`을 동일 디렉터리 임시 파일에서 원자적으로 교체하도록 구현했다.
- 오늘을 포함한 최근 7일은 유지하고 오래된 날짜 파일만 삭제하도록 구현했다.
- 조회·상태·설정·health·ready endpoint와 최근 날짜·국가별 조회를 구현했다.
- 잘못된 날짜·국가, 범위 밖 날짜, 데이터 부재와 Repository 장애를 HTTP 오류로 구분했다.
- 실제 네트워크 없이 임시 저장소와 fixture로 Repository·HTTP 통합 테스트를 추가했다.

### 주요 변경 파일

- `backend/app/repositories/json_issue_repository.py`: 원자적 저장과 만료 삭제
- `backend/app/api/v1/router.py`: v1 endpoint와 오류 매핑
- `backend/app/main.py`: 주입 가능한 FastAPI application factory
- `backend/tests/test_json_issue_repository.py`: 저장·교체·보관 테스트
- `backend/tests/test_api.py`: HTTP endpoint 통합 테스트
- `backend/pyproject.toml`, `backend/uv.lock`: HTTP 테스트 의존성 고정

### 검증 결과

| 명령 또는 검사 | 결과 |
|---|---|
| `scripts/verify-all.ps1` | PASS |
| Ruff | PASS |
| mypy strict | PASS, 13 source files |
| pytest | PASS, 22 tests |
| Secret scan | PASS |
| 외부 뉴스·LLM 호출 | SKIP, 호출하지 않음 |

### 결정사항

- Pages 정적 JSON과 FastAPI는 동일한 `IssueResult` Schema와 Repository 결과를 사용한다.
- 날짜 범위는 JST의 오늘을 포함한 최근 7일이며 미래 날짜도 거부한다.
- 목표 2는 구현 완료지만 주차 정책에 따라 별도 커밋하지 않고 목표 3과 함께 1주차 PR에 포함한다.
- 고정 토요일 리뷰를 폐지하고 주차 완료 조건 감지 직후 자동 리뷰를 실행하도록 변경했다.

### 문제와 리스크

- FastAPI TestClient에서 향후 HTTP client 전환을 알리는 dependency deprecation warning이 1건 발생한다.
- 실제 뉴스 source adapter와 중복 제거는 아직 구현되지 않았다.

### 다음 작업

- Collector 계약, fixture·실제 source adapter, 국가 내부 중복 제거를 구현한다.

## 日本語

### 本日の目標

- 検証済みissue resultをatomic保存し、7日保管policyとFastAPI v1 endpointを完成する。

### 実施した作業

- 日付別resultと`latest.json`を同一directoryの一時fileからatomic置換するよう実装した。
- 当日を含む直近7日を維持し、古い日付fileだけを削除するよう実装した。
- 取得・状態・設定・health・ready endpointと日付・国別取得を実装した。
- 不正な日付・国、範囲外日付、data不在、Repository障害をHTTP errorとして区別した。
- 実networkなしで一時storageとfixtureを使うRepository・HTTP integration testを追加した。

### 主な変更ファイル

- `backend/app/repositories/json_issue_repository.py`：atomic保存と期限切れ削除
- `backend/app/api/v1/router.py`：v1 endpointとerror mapping
- `backend/app/main.py`：注入可能なFastAPI application factory
- `backend/tests/test_json_issue_repository.py`：保存・置換・保管test
- `backend/tests/test_api.py`：HTTP endpoint integration test
- `backend/pyproject.toml`、`backend/uv.lock`：HTTP test依存関係を固定

### 検証結果

| commandまたは検査 | 結果 |
|---|---|
| `scripts/verify-all.ps1` | PASS |
| Ruff | PASS |
| mypy strict | PASS、13 source files |
| pytest | PASS、22 tests |
| Secret scan | PASS |
| 外部ニュース・LLM呼出 | SKIP、呼出なし |

### 決定事項

- Pages静的JSONとFastAPIは同じ`IssueResult` SchemaとRepository resultを使う。
- 日付範囲はJSTの当日を含む直近7日とし、未来日も拒否する。
- 目標2は実装完了だが、週次policyにより別commitせず目標3と共に1週目PRへ含める。
- 土曜日固定reviewを廃止し、週完了条件の検知直後に自動reviewを実行するよう変更した。

### 問題とリスク

- FastAPI TestClientで将来のHTTP client移行を示すdependency deprecation warningが1件発生する。
- 実ニュースsource adapterと重複排除は未実装である。

### 次の作業

- Collector契約、fixture・実source adapter、国内重複排除を実装する。
