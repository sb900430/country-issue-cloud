# Daily Development Report — 2026-08-04

| Item | Value |
|---|---|
| Date (JST) | 2026-08-04 |
| Development week | week-01-data-collection |
| Branch | `codex/week-01-data-collection` |
| Status | In progress; data read layer completed |

## 한국어

### 오늘의 목표

- 명세의 이슈 결과를 Pydantic v2 Schema로 구현하고 검증된 JSON Repository 읽기 계층을 구성한다.

### 수행한 작업

- `CountryCode`, `IssueStatus`, 기사·TOP 이슈·국가·전체 결과 Schema를 구현했다.
- 알 수 없는 필드, 누락 국가, timezone 없는 시각, 범위 밖 수치와 중복 순위를 거부하도록 했다.
- Repository Protocol과 손상 데이터 전용 예외를 정의했다.
- 날짜별 결과, `latest.json`, 최근 N일의 사용 가능 날짜 조회를 구현했다.
- 기존 fixture loader가 같은 Pydantic Schema를 사용하도록 통합했다.
- 정상·부재·손상·범위·알 수 없는 파일 경로 테스트를 추가했다.
- 1차 결과물을 반응형 웹 URL로 전환하고 Android를 후속 보류 트랙으로 유지하도록 한·일 명세와 개발 가이드를 동기화했다.
- 1차 운영을 GitHub Actions + GitHub Pages로 확정하고 Static JSON/FastAPI를 교체 가능한 DataSource 경계로 설계했다.
- 남은 일정을 3주로 단축하고 주차별 브랜치·커밋·Draft PR 하나를 사용하는 정책으로 전환했다.

### 주요 변경 파일

- `backend/app/schemas/issues.py`: v1 데이터 계약
- `backend/app/repositories/base.py`: Repository Protocol과 오류 계약
- `backend/app/repositories/json_issue_repository.py`: 검증된 JSON 읽기 구현
- `backend/app/core/fixtures.py`: 공통 Schema 기반 fixture loader
- `backend/tests/test_issue_schemas.py`: Schema 정상·오류 검증
- `backend/tests/test_json_issue_repository.py`: Repository 조회와 오류 테스트

### 검증 결과

| 명령 또는 검사 | 결과 |
|---|---|
| `.\scripts\verify-all.ps1` | PASS |
| Ruff | PASS |
| mypy strict | PASS, 9 source files |
| pytest | PASS, 13 tests |
| Secret scan | PASS |
| 외부 API·LLM 호출 | SKIP, 호출하지 않음 |

### 결정사항

- Repository는 손상 데이터와 파일 부재를 구분한다. 부재는 `None`, 손상은 `RepositoryDataError`다.
- 최근 날짜 계산은 `Asia/Tokyo` 기준이고 테스트에서는 날짜 제공자를 주입한다.
- 오늘은 읽기 계층만 완료하고 원자적 쓰기와 만료 삭제는 내일 구현한다.
- GitHub Pages는 `StaticJsonDataSource`를 기본 사용하고 FastAPI `ApiDataSource`는 로컬 검증과 후속 VPS/EC2 전환용으로 유지한다.
- Android는 공개 웹 안정화 후 명시적으로 재개할 때까지 구현·배포하지 않는다.

### 문제와 리스크

- `JsonIssueRepository`는 아직 `save`와 `delete_expired`를 구현하지 않아 목표 2가 완료되지 않았다.
- FastAPI endpoint와 HTTP 오류 매핑은 2026-08-05 작업이다.

### 다음 작업

- 원자적 JSON 저장, `latest.json` 갱신, 7일 보관·만료 삭제와 FastAPI v1 endpoint를 구현한다.

## 日本語

### 本日の目標

- 仕様のissue resultをPydantic v2 Schemaで実装し、検証済みJSON Repositoryの読み取り層を構成する。

### 実施した作業

- `CountryCode`、`IssueStatus`、記事・TOP issue・国・全体result Schemaを実装した。
- 未知field、国不足、timezoneなし時刻、範囲外数値、重複順位を拒否するようにした。
- Repository Protocolと破損data専用例外を定義した。
- 日付別result、`latest.json`、直近N日の利用可能日取得を実装した。
- 既存fixture loaderが同じPydantic Schemaを使うよう統合した。
- 正常・不在・破損・範囲・未知file経路のtestを追加した。
- 初回成果物をresponsive Web URLへ変更し、Androidを後続保留trackとして維持するよう韓日仕様と開発ガイドを同期した。
- 初回運用をGitHub Actions + GitHub Pagesへ確定し、Static JSON/FastAPIを交換可能なDataSource境界として設計した。
- 残り日程を3週間へ短縮し、週ごとにbranch・commit・Draft PRを一つ使うpolicyへ変更した。

### 主な変更ファイル

- `backend/app/schemas/issues.py`：v1 data契約
- `backend/app/repositories/base.py`：Repository Protocolとerror契約
- `backend/app/repositories/json_issue_repository.py`：検証済みJSON読み取り実装
- `backend/app/core/fixtures.py`：共通Schemaベースfixture loader
- `backend/tests/test_issue_schemas.py`：Schema正常・異常検証
- `backend/tests/test_json_issue_repository.py`：Repository取得とerror test

### 検証結果

| コマンドまたは検査 | 結果 |
|---|---|
| `.\scripts\verify-all.ps1` | PASS |
| Ruff | PASS |
| mypy strict | PASS、9 source files |
| pytest | PASS、13 tests |
| Secret scan | PASS |
| 外部API・LLM呼出 | SKIP、呼び出しなし |

### 決定事項

- Repositoryは破損dataとfile不在を区別する。不在は`None`、破損は`RepositoryDataError`とする。
- 直近日付計算は`Asia/Tokyo`基準で、testでは日付providerを注入する。
- 本日は読み取り層だけを完了し、atomic書き込みと期限切れ削除は翌日に実装する。
- GitHub Pagesは`StaticJsonDataSource`を標準利用し、FastAPI `ApiDataSource`はローカル検証と後続VPS/EC2移行用に維持する。
- Androidは公開Web安定化後に明示的に再開するまで実装・配布しない。

### 問題とリスク

- `JsonIssueRepository`はまだ`save`と`delete_expired`を実装していないため、目標2は未完了である。
- FastAPI endpointとHTTP error mappingは2026-08-05の作業である。

### 次の作業

- atomic JSON保存、`latest.json`更新、7日保管・期限切れ削除、FastAPI v1 endpointを実装する。
