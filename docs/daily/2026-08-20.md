# Daily Development Report — 2026-08-20

| Item | Value |
|---|---|
| Date (JST) | 2026-08-20 |
| Milestone | keyword pipeline reliability |
| Branch | `codex/keyword-pipeline-reliability` |
| Status | Completed |

## 한국어

### 오늘의 목표

- 기존 무료 뉴스 소스를 유지하면서 필터 이후 기사 수 감소와 재배포 기사 과대 집계를 함께 개선한다.

### 수행한 작업

- 같은 URL만 제거하고 제목·URL 경로·12시간 범위로 재배포 기사를 독립 사건에 군집화했다.
- 발행처 편중 기사를 삭제하지 않고 20%/30건 순위 기여 가중치를 적용했다.
- 키워드 빈도·비율·중복 이슈 판정을 사건 단위로 변경하고 관련 기사 링크는 보존했다.
- 단독 일반어와 일반어 조합을 차단하되 구체 명사와 결합한 복합어는 후보로 유지하고, 같은 근거에서는 짧은 단어를 우선했다.
- URL 고유 기사 50건과 독립 사건 30건 게이트, 실패 사유 코드, 진단 Schema 1.2를 추가했다.
- 한국어·일본어 명세, ADR, 개발 상태를 동일한 결정으로 갱신했다.

### 주요 변경 파일

- `backend/app/batch/deduplication.py`: URL 중복 제거, 사건 군집, 비파괴 매체 가중치
- `backend/app/batch/keywords.py`: 사건 빈도와 일반어·복합어 순위 규칙
- `backend/app/batch/collection_diagnostics.py`: 사건 수와 가중 유효량 진단
- `PROJECT_SPEC.md`, `PROJECT_SPEC_JA.md`: 게시·집계 정책 동기화

### 검증 결과

| 명령 또는 검사 | 결과 |
|---|---|
| 관련 collection·keyword pytest | PASS |
| Ruff·mypy | PASS |
| `scripts/verify-all.ps1` | PASS — Python 172개·Web 14개, coverage 90% |

### 결정사항

- 뉴스 수집원과 24시간 범위는 유지하며 기사 삭제 대신 가중치와 사건 군집으로 표본 품질을 제어한다.
- 화면의 한 단어 우선 원칙을 유지하고 일반어가 포함된 복합어는 근거 후보로만 보존한다.
- 완료 리뷰에서 같은 URL 경로만으로 다른 사건이 합쳐지지 않도록 제목 문맥 조건을 추가하고 실패 사유 분류 테스트를 보강했다.

### 문제와 리스크

- 제목 기반 사건 군집은 오탐·미탐 가능성이 있어 실제 운영 진단의 사건 수를 관찰해야 한다.

### 다음 작업

- commit·push·Draft PR 승인 후 운영 실행에서 국가별 기사·사건 수를 확인한다.

## 日本語

### 本日の目標

- 既存の無料news sourceを維持しながら、filter後の記事数減少と再配信記事の過大集計を同時に改善する。

### 実施した作業

- 同一URLだけを除去し、title・URL path・12時間範囲で再配信記事を独立storyへclusteringした。
- 媒体偏重記事を削除せず、20%/30件の順位寄与weightを適用した。
- keyword頻度・ratio・重複イシュー判定をstory単位へ変更し、関連記事linkは保存した。
- 単独一般語と一般語同士の組合せを遮断し、具体名詞と結合した複合語は候補として維持して、同じ根拠では短い単語を優先した。
- URLユニーク記事50件と独立story 30件gate、失敗理由code、診断Schema 1.2を追加した。
- 韓国語・日本語仕様、ADR、開発状態を同じ決定内容で更新した。

### 主な変更ファイル

- `backend/app/batch/deduplication.py`: URL重複排除、story clustering、非破壊媒体weight
- `backend/app/batch/keywords.py`: story頻度と一般語・複合語順位規則
- `backend/app/batch/collection_diagnostics.py`: story数とweighted有効量の診断
- `PROJECT_SPEC.md`, `PROJECT_SPEC_JA.md`: 配布・集計policy同期

### 検証結果

| コマンドまたは検査 | 結果 |
|---|---|
| 関連collection・keyword pytest | PASS |
| Ruff・mypy | PASS |
| `scripts/verify-all.ps1` | PASS — Python 172件・Web 14件、coverage 90% |

### 決定事項

- news sourceと24時間範囲は維持し、記事削除ではなくweightとstory clusteringでsample品質を制御する。
- 画面の一単語優先原則を維持し、一般語を含む複合語は根拠候補として保存する。
- 完了reviewで同じURL pathだけにより別storyが統合されないようtitle文脈条件を追加し、失敗理由分類testを補完した。

### 問題とリスク

- title基準のstory clusteringには誤検出・見逃しの可能性があり、実運用診断のstory数を観察する必要がある。

### 次の作業

- commit・push・Draft PR承認後、運用実行で国別記事・story数を確認する。
