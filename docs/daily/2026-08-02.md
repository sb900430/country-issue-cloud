# Daily Development Report — 2026-08-02

| Item | Value |
|---|---|
| Date (JST) | 2026-08-02 |
| Milestone | Pre-development security hardening |
| Branch | `main` |
| Status | Completed |

## 한국어

### 오늘의 목표

- API·LLM·배포 자격증명이 Git에 포함되지 않도록 파일별 관리 정책과 자동 검사를 마련한다.

### 수행한 작업

- 한·일 명세에 파일별 Secret 관리표와 커밋 차단 기준을 동기화했다.
- 환경파일, 서명키, 서비스 계정, 로컬 데이터와 로그를 `.gitignore`에 추가했다.
- 금지 파일명과 고신뢰 key 패턴을 검사하는 `scripts/check-secrets.ps1`을 추가했다.
- Secret 검사를 `scripts/verify-all.ps1`과 AI 개발 완료 조건에 연결했다.
- PR과 `main` push에서 같은 검사를 실행하는 GitHub Actions workflow를 추가했다.
- GitHub 저장소의 기본 Secret Scanning과 Push Protection 활성화 상태를 확인했다.

### 주요 변경 파일

- `PROJECT_SPEC.md`, `PROJECT_SPEC_JA.md`: Secret 관리와 차단 기준
- `.gitignore`: 민감 파일과 로컬 산출물 제외
- `scripts/check-secrets.ps1`: 로컬 Secret 검사
- `scripts/verify-all.ps1`: 공통 검증에 Secret 검사 연결
- `docs/AI_DEVELOPMENT_GUIDE.md`, `docs/AI_DEVELOPMENT_GUIDE_JA.md`: 완료 조건과 표준 명령 갱신
- `.github/workflows/secret-protection.yml`: PR·main Secret 보호 CI

### 검증 결과

| 명령 또는 검사 | 결과 |
|---|---|
| `.\scripts\verify-all.ps1` | PASS; 미생성 backend/frontend/Android 검사는 SKIP |
| `git diff --check` | PASS |

### 결정사항

- Android에는 공개 가능한 백엔드 URL과 공개용 Client ID만 허용하고 provider Secret은 백엔드·배포 환경에만 보관한다.
- 로컬 고신뢰 패턴 검사와 GitHub 서버 측 Secret scanning/push protection을 함께 사용한다.
- GitHub의 비제공자 패턴과 key 유효성 검사는 현재 저장소에서 활성화 요청 후에도 비활성 상태로 유지되어 기본 기능과 CI 검사를 사용한다.

### 문제와 리스크

- GitHub의 추가 Secret Scanning 기능은 계정·플랜 지원 범위에 따라 활성화되지 않을 수 있다.

### 다음 작업

- 스캐폴드 생성 시 값이 비어 있는 `backend/.env.example`과 환경변수 Schema 검증을 구현한다.

## 日本語

### 本日の目標

- API・LLM・配布credentialがGitへ含まれないよう、ファイル別管理policyと自動検査を整備する。

### 実施した作業

- 韓国語・日本語仕様へファイル別Secret管理表とcommitブロック基準を同期した。
- 環境ファイル、署名key、service account、ローカルdata、logを`.gitignore`へ追加した。
- 禁止ファイル名と信頼度の高いkey patternを検査する`scripts/check-secrets.ps1`を追加した。
- Secret検査を`scripts/verify-all.ps1`とAI開発の完了条件へ接続した。
- PRと`main`へのpushで同じ検査を実行するGitHub Actions workflowを追加した。
- GitHub repositoryの標準Secret ScanningとPush Protectionが有効であることを確認した。

### 主な変更ファイル

- `PROJECT_SPEC.md`、`PROJECT_SPEC_JA.md`：Secret管理とブロック基準
- `.gitignore`：機密ファイルとローカル生成物の除外
- `scripts/check-secrets.ps1`：ローカルSecret検査
- `scripts/verify-all.ps1`：共通検証へのSecret検査接続
- `docs/AI_DEVELOPMENT_GUIDE.md`、`docs/AI_DEVELOPMENT_GUIDE_JA.md`：完了条件と標準command更新
- `.github/workflows/secret-protection.yml`：PR・main用Secret保護CI

### 検証結果

| コマンドまたは検査 | 結果 |
|---|---|
| `.\scripts\verify-all.ps1` | PASS、未作成のbackend/frontend/Android検査はSKIP |
| `git diff --check` | PASS |

### 決定事項

- Androidには公開可能なbackend URLと公開用Client IDだけを許可し、provider Secretはbackend・配布環境だけに保存する。
- ローカルの高信頼pattern検査とGitHub server側のSecret scanning/push protectionを併用する。
- GitHubのnon-provider patternとkey validity checkは有効化要求後も無効のままであるため、標準機能とCI検査を利用する。

### 問題とリスク

- GitHubの追加Secret Scanning機能はaccount・planの対応範囲によって有効化できない場合がある。

### 次の作業

- scaffold作成時に値を空にした`backend/.env.example`と環境変数Schema検証を実装する。
