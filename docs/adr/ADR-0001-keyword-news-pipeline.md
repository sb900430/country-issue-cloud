# ADR-0001: 국가별 대량 경제뉴스 기반 키워드 파이프라인 / 国別大量経済ニュースに基づくkeyword pipeline

- 상태 / 状態: Accepted
- 결정일 / 決定日: 2026-08-06
- 관련 명세 / 関連仕様: `PROJECT_SPEC.md`, `PROJECT_SPEC_JA.md`

## 한국어

### 배경

기존 운영 소스는 중앙은행·정부기관 RSS 중심이어서 국가별 하루 기사량이 적고 일반 경제언론의 공통 관심사를 대표하지 못했다. 사용자가 원하는 결과는 기관 발표 요약이 아니라, 각국 경제뉴스 100건 이상에서 반복 출현한 단어·복합명사 TOP 5와 키워드별 관련 기사다.

### 결정

1. GDELT DOC API Article List를 미국·일본·한국의 주 뉴스 metadata 소스로 사용한다.
2. 국가별 중복 제거 후 150건을 목표로 하고 최대 250건, 권장 수집 100건 이상, 당분간 게시 하한 50건으로 제한한다. 49건 이하는 표본 미달이다.
3. 기존 중앙은행·정부 RSS와 BEA·e-Stat 같은 공공 API는 보조 소스로 유지한다.
4. 기사 본문을 crawl·저장하지 않고 제공된 제목·짧은 요약·URL·매체·시각만 사용한다.
5. 영어·일본어·한국어별 분석기가 반복 명사와 최대 2개 형태소의 짧은 복합명사를 추출하고 국가별 불용어와 문장 조각을 제거한다.
6. 동일 기사의 반복 출현은 한 번만 계산하며, 고유 기사 문서 빈도를 우선하고 매체 다양성·최신 시각·ID로 동률을 해소한다.
7. 로컬 다국어 임베딩은 입력 후보 안에서 고신뢰 동의어·표기 변형을 통합하고, 외부 LLM은 기본 비활성인 후속 표시명 보정 경계로만 유지한다.
8. TOP 5 키워드는 근거 표현과 관련 기사 최대 20개를 제공한다.
9. 기존 v1 필드 의미를 바꾸지 않고 키워드 계약을 Schema 2.0, `/api/v2`, `data/v2`로 추가한다.
10. 원문 `label`은 분석 기준으로 고정하고 순위 완료 후 미국·일본에 한국어 `label_ko`를 붙인다. 1차 provider는 version 관리되는 완전 일치 사전이며 외부 번역 API·유료 LLM을 호출하지 않는다.

### 결과와 트레이드오프

- 기사 표본과 매체 다양성이 늘어 사용자가 기대한 “그날 많이 등장한 경제 키워드”에 가까워진다.
- 국가별 query와 불용어가 편향을 만들 수 있으므로 version·수집량·표본 품질을 기록하고 fixture 회귀 검증을 수행한다.
- 형태소 분석 의존성과 v2 migration 작업이 추가되지만, 결정적 순위와 관련 기사 근거를 유지해 설명 가능성을 높인다.
- GDELT 가용성이나 포괄 범위가 변할 수 있으므로 기존 RSS adapter와 DataSource 경계를 유지한다.
- 무료 사전은 결정적이고 검토 가능하지만 새 표현을 자동 번역하지 못한다. 미등록 표현은 원문으로 fallback하고 사전 보강 이력은 Git에서 검토한다.

### 구현 순서

1. GDELT adapter·국가별 query config·100건 이상 fixture
2. 중복 제거와 매체 편중 제한
3. 언어별 후보 추출·불용어·복합명사 검증
4. 로컬 임베딩 제한 통합과 결정적 순위
5. Schema/API/DataSource v2와 관련 기사 UI
6. live 표본 품질·비용·성능 검증 후 운영 전환

2026-08-08 실제 표본에서 제목 앞부분이 문장 조각으로 노출되는 한계가 확인되어 한국어는 `kiwipiepy`, 일본어는 `SudachiPy` core 사전, 영어는 정규화 단어·2단어 규칙으로 교체했다. 한 제목에서 여러 후보를 만들되 하나의 단어 또는 최대 2개 형태소의 짧은 이슈 개념만 허용한다. 후보는 최소 3개 기사 또는 전체의 3% 중 큰 값과 서로 다른 2개 매체를 충족해야 한다. 국가별 120건 fixture에서 짧은 TOP 5, 일반어·문장 조각 제외, 결정성, 원문 근거와 관련 기사 최대 20건을 회귀 검증한다.

2026-08-09 실제 예약 실행에서 US 198건·JP 103건·KR 85건이 확보됐지만 기존 100건 게시 게이트로 결과가 폐기됐다. 무료 소스의 일별 변동을 수용하면서 TOP 5 품질 조건을 유지하기 위해 수집 목표는 그대로 두고 세 국가의 실제 게시 하한만 각각 70건으로 낮춘다.

2026-08-13 운영 관찰에서는 일본 원본 128건·중복 제거 125건 중 `Investing- Fx` 96건이 매체 다양성 제한으로 축소되어 최종 55건만 남았다. 무료 소스의 일별 편차를 먼저 관찰하기 위해 게시 하한만 당분간 50건으로 낮추고, 권장 수집량·목표량·매체 다양성·TOP 5 품질 기준은 유지한다. 50~99건 공개 결과는 매일 국가별 건수와 품질을 보고해 하한 복원의 근거로 사용한다.

2026-08-10 실제 게시 결과는 기사 수를 충족해도 반복 템플릿 일반어, 날짜·통화 단위, 유사한 관련 기사 집합이 TOP 5를 차지할 수 있음을 보여줬다. 후보 하한을 최소 4개 기사 또는 전체의 5%로 강화하고, 날짜·단위·템플릿 일반어를 제외하며, 관련 기사 집합 Jaccard 유사도 0.5 이상인 후순위 후보를 제거한다. NewsData.io는 반복 템플릿 매체 차단과 일본어 경제 제목 용어 gate를 적용한다. GDELT는 반복 429가 해소될 때까지 운영에서 일시 비활성화한다. 새 결과가 실패하거나 `main` push가 발생해도 공개 이력이 fixture로 덮이지 않도록 현재 공개 Schema 2.0 데이터를 복원해 최근 7일을 재게시한다. 관리자는 공개되지 않는 7일 보관 Actions artifact에서 최종 선택 기사 metadata와 진단을 확인한다.

2026-08-11 운영 표본에서 정치인·정당명이 경제 키워드 후보로 남는 문제를 확인했다. 코드 상수의 무기한 증가 대신 `config/keyword-blocklist.yml`에 국가별 `exact`·`contains` 규칙, 분류, 한·일 사유, 추가일과 활성 상태를 기록한다. 후보는 문서 빈도 집계 전에 차단하며 잘못된 설정은 조용히 무시하지 않고 배치를 실패시켜 기존 정상 배포를 보존한다. 최초 한국 규칙으로 `국힘`, `오세훈`을 등록한다.

2026-08-14 실제 24시간 표본에서 복합어 구성 단어가 제외되고 정확한 문자열별로 후보가 분산되어 US·KR은 품질 gate를 통과한 후보가 0개, JP는 4개에 그쳤다. 구성 단어와 복합어를 함께 보존하고, 외부 API가 필요 없는 `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`로 같은 국가의 고신뢰 후보만 병합한다. 재현 가능한 결과를 위해 검증한 model revision `e8f8c211226b894fcb81acc59f3b34ba3efd5f42`를 고정한다. 짧은 표현 과병합을 막기 위해 각 후보 2기사·2매체, 공백 제외 4자 이상, cosine 유사도 0.95 이상, cluster 최대 3표현을 요구한다. 최종 후보 gate는 최소 3기사 또는 전체 2% 중 큰 값과 2매체로 조정한다. 실제 저장 표본 재검증에서 US `inflation`·`revenue`·`power`·`growth`·`claim`, JP `国債`·`金利`·`ドル`·`通信`·`入札`, KR `공급`·`부동산`·`주택`·`서울`·`대책`의 TOP 5를 생성했다.

2026-08-19 최근 운영 표본에서는 기사 수가 충분해도 일반 영어 동사·요일·회사 접미사, 일본 언론사명·숫자, 한국 정치인·지역명이 높은 빈도로 상위에 남았다. 품질이 낮은 후보로 5개를 강제 완성하는 정책을 중단하고, 국가별 최소 3개·최대 5개의 품질 후보만 성공으로 인정한다. local embedding은 후보명 통합 외에 같은 후보 관련 기사 제목의 평균 cosine 응집도를 순위에 반영하고 중앙 기사부터 관련 기사로 제공한다. 매체명은 정규화하고 보도자료 wire 계열을 같은 family로 묶으며, 매체별 20% 제한은 최종 선택 가능 수에 수렴하도록 계산한다. 일반어는 언어별 기본 목록, 정치인·정당명은 국가별 YAML 금지 목록으로 관리한다. 이 변경으로 저장된 8월 17~19일 표본에서 미국의 무의미한 후보만 남은 날짜는 미국만 안전하게 실패하고, 일본·한국의 기준 통과 결과는 독립 게시할 수 있다.

2026-08-19 사용자 표시 언어는 전체 UI locale이 아니라 keyword 원문/한국어 전환으로 정의한다. `KeywordTranslator` 경계 뒤의 `config/keyword-translations.yml`을 Schema 검증하고 정규화 완전 일치로 조회한다. 번역은 문서 빈도·clustering·순위가 모두 끝난 뒤에만 수행하며, 한국 keyword와 미등록 미국·일본 keyword는 원문을 반환한다. Schema 2.0의 `label_ko`는 기존 공개 이력 호환을 위해 optional로 추가하되 새 batch 결과는 항상 값을 기록한다. Web 기본값은 한국어이며 원문 전환은 추가 network 요청 없이 타일·cloud·상세 title에 함께 적용한다.

## 日本語

### 背景

既存の本番sourceは中央銀行・政府機関RSS中心で、国別の日次記事数が少なく、一般経済報道の共通関心を代表できなかった。利用者が求める結果は機関発表の要約ではなく、各国の経済ニュース100件以上から反復出現する単語・複合名詞TOP 5とkeyword別関連記事である。

### 決定

1. GDELT DOC API Article Listを米国・日本・韓国の主news metadata sourceとして使う。
2. 国別重複排除後150件を目標、最大250件、推奨収集100件以上、当面の配布下限50件とする。49件以下はsample不足とする。
3. 既存中央銀行・政府RSSとBEA・e-Statなどの公共APIは補助sourceとして維持する。
4. 記事本文をcrawl・保存せず、提供title・短いsummary・URL・媒体・時刻だけを使う。
5. 英語・日本語・韓国語別分析器が反復名詞と最大2形態素の短い複合名詞を抽出し、国別stopwordと文断片を除外する。
6. 同一記事内の反復出現は一度だけ数え、ユニーク記事のdocument frequencyを優先し、媒体多様性・最新時刻・IDで同順位を解消する。
7. local多言語embeddingは入力候補内の高信頼な同義語・表記揺れを統合し、外部LLMは標準無効の後続表示名補正境界としてのみ維持する。
8. TOP 5 keywordは根拠表現と関連記事最大20件を提供する。
9. 既存v1 field意味を変更せず、keyword契約をSchema 2.0、`/api/v2`、`data/v2`として追加する。
10. 原文`label`は分析基準として固定し、順位確定後に米国・日本へ韓国語`label_ko`を付与する。初回providerはversion管理された完全一致辞書とし、外部翻訳API・有料LLMを呼び出さない。

### 結果とtrade-off

- 記事sampleと媒体多様性が増え、利用者が期待する「当日多く登場した経済keyword」に近づく。
- 国別queryとstopwordが偏りを生む可能性があるため、version・収集量・sample品質を記録しfixture regressionを行う。
- 形態素分析依存とv2 migration作業が増えるが、決定的順位と関連記事根拠を維持して説明可能性を高める。
- GDELTの可用性・coverage変更に備え、既存RSS adapterとDataSource境界を維持する。
- 無料辞書は決定的でreview可能だが、新しい表現を自動翻訳できない。未登録表現は原文へfallbackし、辞書追加履歴をGitでreviewする。

### 実装順序

1. GDELT adapter・国別query config・100件以上fixture
2. 重複排除と媒体偏重制限
3. 言語別候補抽出・stopword・複合名詞検証
4. local embedding限定統合と決定的順位
5. Schema/API/DataSource v2と関連記事UI
6. live sample品質・費用・性能検証後に本番移行

2026-08-08の実sampleでtitle先頭部分が文断片として表示される限界を確認し、韓国語は`kiwipiepy`、日本語は`SudachiPy` core辞書、英語は正規化単語・2語規則へ置き換えた。一つのtitleから複数候補を生成するが、一単語または最大2形態素の短いイシュー概念だけを許可する。候補は最低3記事または全体の3%の大きい方と、異なる2媒体を満たす必要がある。国別120件fixtureで短いTOP 5、一般語・文断片除外、決定性、原文根拠、関連記事最大20件をregression検証する。

2026-08-09の実予約実行ではUS 198件・JP 103件・KR 85件を確保したが、従来の100件配布gateにより結果が破棄された。無料sourceの日次変動を許容しながらTOP 5品質条件を維持するため、収集目標は維持し、3か国の実配布下限だけを各70件へ下げる。

2026-08-13の運用観察では、日本のraw 128件・重複排除後125件のうち`Investing- Fx` 96件が媒体多様性制限により縮小され、最終55件だけが残った。無料sourceの日次偏差を先に観察するため、配布下限だけを当面50件へ下げ、推奨収集量・目標量・媒体多様性・TOP 5品質基準は維持する。50～99件で公開した結果は毎日国別件数と品質を報告し、下限復元の根拠として使う。

2026-08-10の実配布結果により、記事数を満たしても反復template一般語、日付・通貨単位、類似関連記事集合がTOP 5を占め得ることが分かった。候補下限を最低4記事または全体の5%へ強化し、日付・単位・template一般語を除外して、関連記事集合Jaccard類似度0.5以上の後順位候補を取り除く。NewsData.ioには反復template媒体の遮断と日本語経済title用語gateを適用する。GDELTは反復429が解消されるまで運用で一時無効とする。新結果の失敗や`main` pushでも公開履歴がfixtureで上書きされないよう、現在公開中のSchema 2.0 dataを復元して直近7日を再配布する。管理者は非公開・7日保持のActions artifactで最終選択記事metadataと診断を確認する。

2026-08-11の運用sampleで政治家名・政党名が経済keyword候補に残る問題を確認した。code定数を増やし続ける代わりに、`config/keyword-blocklist.yml`へ国別`exact`・`contains`規則、分類、韓日理由、追加日、有効状態を記録する。候補はdocument frequency集計前に遮断し、不正な設定を黙って無視せずbatchを失敗させて既存正常配布を保護する。最初の韓国規則として`국힘`と`오세훈`を登録する。

2026-08-14の実24時間sampleで複合語の構成単語が除外され、完全一致文字列ごとに候補が分散したため、US・KRは品質gate通過候補0件、JPは4件に留まった。構成単語と複合語を両方保持し、外部API不要の`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`で同一国内の高信頼候補だけを統合する。再現可能な結果のため、検証済みmodel revision `e8f8c211226b894fcb81acc59f3b34ba3efd5f42`を固定する。短い表現の過統合を防ぐため、各候補2記事・2媒体、空白除外4文字以上、cosine類似度0.95以上、cluster最大3表現を要求する。最終候補gateは最低3記事または全体2%の大きい方と2媒体へ調整する。保存済み実sampleの再検証ではUS `inflation`・`revenue`・`power`・`growth`・`claim`、JP `国債`・`金利`・`ドル`・`通信`・`入札`、KR `공급`・`부동산`・`주택`・`서울`・`대책`のTOP 5を生成した。

2026-08-19の最近の運用sampleでは、記事数が十分でも一般的な英語動詞・曜日・会社suffix、日本の媒体名・数字、韓国の政治家名・地域名が高頻度で上位に残った。低品質候補で5件を強制完成する方針を廃止し、国別最低3件・最大5件の品質候補だけを成功とする。local embeddingは候補名統合に加え、同じ候補の関連記事title平均cosine凝集度を順位へ反映し、中心記事から関連記事として提供する。媒体名を正規化してpress release wire系列を同じfamilyにまとめ、媒体別20%制限は最終選択可能数へ収束するよう計算する。一般語は言語別標準list、政治家・政党名は国別YAML禁止listで管理する。この変更により保存済み8月17～19日sampleで、米国の無意味な候補だけが残る日は米国だけを安全に失敗させ、日本・韓国の基準通過結果を独立公開できる。

2026-08-19の利用者向け表示言語はUI全体のlocaleではなく、keyword原文/韓国語切替として定義する。`KeywordTranslator`境界の後ろで`config/keyword-translations.yml`をSchema検証し、正規化完全一致で検索する。翻訳はdocument frequency・clustering・順位がすべて完了した後だけ実行し、韓国keywordと未登録の米国・日本keywordは原文を返す。Schema 2.0の`label_ko`は既存公開履歴互換のためoptional追加とするが、新batch結果は常に値を記録する。Web標準は韓国語で、原文切替は追加network requestなしにtile・cloud・詳細titleへ同時適用する。
